# v3.5.4 — Remove Project Management Showpiece

- Removes the legacy empty `Project Management` placeholder even if an older build created default views for it.
- Hides any zero-task legacy `Project Management` tile from the Projects sidebar immediately.
- Does not create any demo project for new workspaces.
- User-created/AI-created real projects remain visible.
