# v3.4.1 Avatar / Work Status UI Fix

- Fixed the top-right work-status badge covering the profile avatar.
- Status is now an 8px Slack-style corner dot with a topbar-colored ring.
- Avatar initials stay visible when no profile image is configured.
- If `avatar_url` exists on the signed-in account, the topbar and profile dialog render the actual profile image.
- Scoped avatar CSS so the avatar body and status dot no longer inherit the same dimensions/background.
- Existing v3.4 AI auto-assignment, work-status logic, Turso/SQLite persistence, real-time chat and task planner behavior are unchanged.
